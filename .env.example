# ==========================================
# Privacy Data Layer - Environment Variables
# ==========================================

# Database Configuration (Redis)
# Default host for local Docker setup is 'localhost'
REDIS_HOST=localhost

# Default port for Redis is 6379
REDIS_PORT=6379

# Security Configuration
# (Add any future secret keys or vault passwords here)